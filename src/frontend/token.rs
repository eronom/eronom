#[derive(Debug, Clone, PartialEq)]
pub enum TokenType {
    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    LeftBracket,
    RightBracket,
    Comma,
    Dot,
    Minus,
    Plus,
    Colon,
    Slash,
    Star,
    Bang,
    BangEqual,
    Equal,
    EqualEqual,
    Greater,
    GreaterEqual,
    Less,
    LessEqual,
    Arrow,
    DotDot,
    Identifier(String),
    String(String),
    Number(f64),
    And,
    Else,
    False,
    For,
    If,
    Null,
    Or,
    Print,
    Return,
    True,
    Let,
    Const,
    In,
    Import,
    Export,
    From,
    Function,
    Struct,
    Embed,
    Interface,
    Concurrent,
    Spawn,
    On,
    While,
    Break,
    Continue,
    Try,
    Catch,
    Finally,
    Throw,
    Switch,
    Case,
    Default,
    Match,
    Underscore,
    Eof,
}

#[derive(Debug, Clone)]
pub struct Token {
    pub ty: TokenType,
    pub line: usize,
    pub col: usize,
}
